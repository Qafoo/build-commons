<?xml version="1.0" encoding="UTF-8" ?>
<project name="abc">
    <dirname property="abc:base.directory"
             file="${ant.file.abc}" />

    <!-- Just to have extension points available -->
    <!-- TODO: Move extension points into a separate file -->
    <import file="${abc:base.directory}/Base/Lifecycle.xml" />
    <import file="${abc:base.directory}/Configure/Configure.xml" />
</project>
