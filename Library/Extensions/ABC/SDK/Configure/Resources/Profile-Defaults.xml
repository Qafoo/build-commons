<?xml version="1.0" encoding="UTF-8"?>
<profile-defaults>
    <!-- Loaded during bootstrap anyways -->
    <profile default="disabled">ABC:SDK:Ant-Contrib</profile>
    <profile default="disabled">ABC:SDK:Configure</profile>
    <profile default="disabled">ABC:SDK:Core</profile>
    <profile default="disabled">ABC:SDK:String</profile>

    <!-- Needed during runtime -->
    <profile default="enabled">ABC:SDK:Lifecycle</profile>
    <profile default="enabled">ABC:SDK:Changes</profile>
    <profile default="enabled">ABC:SDK:Execution</profile>

    <!-- Mandatory environments -->
    <!-- TODO: Auto generate the required environments based on project dependencies (in the *.xslt) -->
    <!--<profile default="enabled">ABC:Environment:PHP</profile>-->
    <!--<profile default="enabled">ABC:Environment:Rhino</profile>-->
    <!--<profile default="enabled">ABC:Environment:nodejs</profile>-->
    <profile default="enabled">ABC:Environment:PHP:Composer</profile>
    <profile default="enabled">ABC:Environment:PHP:PHPUnit</profile>
</profile-defaults>
